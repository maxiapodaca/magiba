<% answers = @comentario.answers.all%>
<% if !answers.empty? %>
	<% answers.each do |a| %>
		<table class="table table-hover">
		<tr>
			<td>
			</td>
			<td>
				<font face="arial" size="3" color="black">
					<%= a.user.name %>
				</font>
				<font face="arial" size="2" color="grey">
				    respondió el <%= a.created_at.day %>/<%= a.created_at.month %>/<%= a.created_at.year %> a las <%= a.created_at.hour %>:<%= a.created_at.min %> 
				</font>
			</td>
		</tr>
		<tr>
			<td>
			</td>
			<td>
				<font face="arial" size="3" color="grey">
					<%= a.detalle %>
				</font>
			</td>
		</tr>
	</table>
	<% end %>
<% end %>